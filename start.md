# 🚀 DeepSeek Harness 桌面版 · 新手安装指南

> 本指南写给第一次接触的朋友。跟着步骤做，10 分钟就能装好。遇到问题请看文末「常见问题」。

## 这是什么？

DeepSeek Harness 是深度求索（DeepSeek）出品的 AI 编程助手平台。本项目的**桌面版**是一个"外壳"：像普通软件一样双击打开，它会自动帮你把后台服务跑起来，用起来更省心。

## 安装前，请确认

- 电脑系统是 **Windows 10 或 Windows 11**
- 电脑能正常上网

## 第 1 步：下载安装包

1. 打开网页：<https://github.com/emomg/deepseek-harness-desktop>
2. 点击页面右侧的 **Releases**（发行版）
3. 找到最新版本，下载 `DeepSeek-Harness-Desktop-Setup-0.1.0.exe`（约 2MB）

## 第 2 步：安装 Node.js（没装过才需要）

桌面版的后台服务需要 Node.js 才能运行。

1. 打开 <https://nodejs.org/zh-cn>
2. 点击绿色的 **LTS** 版本按钮下载
3. 双击下载好的安装包，一路点「下一步」装完（用默认设置即可）

> **怎么检查有没有装过？** 按键盘 `Win + R`，输入 `cmd` 回车，再输入 `node -v` 回车。如果显示 `v18` 或更高的版本号，就说明已经装好了，跳过这一步。

## 第 3 步：安装 dsh 命令

1. 按键盘 `Win + R`，输入 `cmd`，回车（打开命令行窗口）
2. 输入下面这行，按回车：

```
npm install -g @deepseek-ai/dsh
```

3. 等它跑完（出现版本号之类的文字就成功了）

## 第 4 步：安装桌面版

1. 双击刚才下载的 `DeepSeek-Harness-Desktop-Setup-0.1.0.exe`
2. 一路点「下一步」→「安装」（安装到你自己账户的目录，**不需要管理员权限**）
3. 安装完成后会自动启动

## 第 5 步：开始使用

- 首次打开：应用会自动启动后台服务并弹出主窗口，稍等几秒即可使用
- 点窗口右上角 **×** 不会退出，而是最小化到**系统托盘**（屏幕右下角的小图标）
- 想彻底退出：右键托盘图标 → 点「退出」
- 下次使用：双击桌面上的「DeepSeek Harness」快捷方式即可

## 常见问题（小白必看）

**问：打开后提示"未找到 dsh"？**
答：说明第 3 步没装好。重新按第 3 步操作，装完后再打开应用。

**问：双击安装包没反应？**
答：先确认下载完整（文件约 2MB）。如果浏览器提示"不安全"，选择"保留"即可；安装包是开源的，源码在本仓库。

**问：需要管理员权限吗？**
答：不需要。安装在自己账户的目录下（`%LOCALAPPDATA%\Programs\DeepSeek Harness`）。

**问：我的聊天记录和密钥安全吗？**
答：安全。所有数据只保存在**你自己电脑**的 `~/.dsh` 文件夹里（聊天记录、密钥、配置都在这里），不会上传到任何地方，也不会随安装包分发。

**问：怎么卸载？**
答：开始菜单 → DeepSeek Harness → 「卸载 DeepSeek Harness」。卸载只删除应用本身，**不会删除**你的数据；想彻底清除数据，再手动删除 `~/.dsh` 文件夹即可。

**问：看不懂报错怎么办？**
答：把报错文字截图发到 Issues：<https://github.com/emomg/deepseek-harness-desktop/issues>，会有人帮你。

## 还想了解更多？

- 功能特性与源码构建：见 [README](README.md)
- 许可：MIT License，Copyright (c) 2026 DeepSeek