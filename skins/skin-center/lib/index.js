// @dsh-desktop/skin-center · 宿主端
// 纯浏览器端插件：没有 host 端路由 / API / 数据。
// 留空 stub 以满足 dsh cordis 插件的入口约定。

export const name = 'dsh-desktop-skin-center';

export const inject = [];

/** host 端无需任何服务，仅注册名称即可。 */
// eslint-disable-next-line no-unused-vars
export function apply(_ctx) {
  /* no-op: all logic is browser-side via client.js */
}
